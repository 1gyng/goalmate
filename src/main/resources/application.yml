spring:
  application:
    name: goalmate
  thymeleaf:
    cache: false
  datasource:
    url: "jdbc:h2:mem:goalmate;MODE=MYSQL;NON_KEYWORDS=USER;DB_CLOSE_DELAY=-1"
    username: "sa"
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show_sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console
---